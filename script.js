document.getElementById('contact-form').addEventListener('submit', function(e) {
    e.preventDefault();
    alert('¡Mensaje enviado con éxito!');
    // Aquí agregar la lógica para enviar el formulario usando fetch o Ajax
});

